#include "main.h"

/**
 * _1supper - check if a letter is supper
 * @x: The number to be checked
 * Return: 1 for supper letter or 0 for any else
 */
 
_1supper(int x)
{
	if (x >= 65 && x <= 90)
	{
	return (1);
	}
	return (0);
}
